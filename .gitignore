# 가상환경 (용량이 크고 각자 만들면 되므로 올리지 않음)
kcf-env/
venv/

# 비밀 키 — 절대 GitHub에 올리면 안 됩니다!
.env

# 파이썬 캐시
__pycache__/
*.py[cod]

# 캐시/다운로드 데이터
data/*.pkl

# ACLED 원본 데이터는 재배포 제한이 있어 GitHub에 올리지 않음 (각자 다운로드)
data/*.csv

# 뉴스/LLM 캐시 (저작권 + 개인 API 사용내역 보호)
data/*.json

# 생성된 스냅샷 이미지 (다시 만들 수 있으므로 올리지 않음)
frames/

# OS / 에디터 부산물
.DS_Store
.vscode/
.idea/
